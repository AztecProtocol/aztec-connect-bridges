version: '2.1'
jobs:
  build:
    docker:
      - image: 'aztecprotocol/build-image'
    steps:
      - setup_remote_docker:
          version: 19.03.13
      - run:
          name: 'Build and test'
          working_directory: .
          command: build client-bridge-data

workflows:
  build:
    jobs:
      - build
